fn main() {
    // Real implementation lands in later tasks. Exit 2 (qpdf's default error
    // code for compare-for-test) so any accidental invocation of the stub
    // fails loud.
    std::process::exit(2);
}
